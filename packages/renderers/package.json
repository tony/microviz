{
  "name": "@microviz/renderers",
  "description": "Framework-agnostic renderers for microviz RenderModel.",
  "version": "0.1.0-next.0",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/renderers.js"
    }
  },
  "sideEffects": false,
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@microviz/core": "workspace:*"
  },
  "scripts": {
    "clean": "rimraf dist",
    "distclean": "rimraf dist node_modules",
    "check": "tsc -p tsconfig.check.json --noEmit",
    "type-check": "tsc -p tsconfig.check.json --noEmit",
    "build": "vite build",
    "build:watch": "pnpm build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome lint .",
    "biome": "biome",
    "format": "pnpm run biome check . --write; pnpm run biome format . --write",
    "update": "pnpm update",
    "ncu": "ncu"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=18"
  }
}
